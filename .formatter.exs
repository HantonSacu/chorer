[
  import_deps: [:absinthe, :ecto, :ecto_enum, :ecto_sql, :phoenix, :surface],
  inputs: [
    "*.{ex,exs}",
    "priv/*/seeds.exs",
    "priv/*/migrations/*.exs",
    "{config,lib,test}/**/*.{ex,exs}"
  ]
]
